{
  "schema": "gofly.zrpc_proto_compatibility.v1",
  "status": "blocking-contract",
  "acceptanceGate": "make zrpc-proto-compatibility-check",
  "sourceOfTruth": [
    "testdata/zrpc-proto-matrix/shop.proto",
    "testdata/zrpc-proto-matrix/common.proto",
    "cmd/gofly/internal/generator/proto_parser.go",
    "cmd/gofly/internal/generator/proto_codegen.go",
    "cmd/gofly/internal/command/rpc_gen_command.go",
    "cmd/gofly/internal/command/rpc_protoc_command.go"
  ],
  "referenceFramework": "goctl rpc protoc / go-zero zRPC",
  "matrix": [
    {
      "id": "external-proto-imports",
      "status": "supported",
      "surface": "proto imports",
      "evidence": ["import \"common.proto\"", "Money", "TraceMeta", "recursive-local-import-merge"],
      "gate": "make zrpc-proto-compatibility-check",
      "reason": "GenerateRPCFromProto recursively parses local proto imports and merges imported message and enum definitions into generated DTOs.",
      "rollbackOrEscalation": "Rollback by preserving local import merge evidence and treating lost imported DTO generation as a supported-row regression."
    },
    {
      "id": "multiple-services",
      "status": "supported",
      "surface": "multi-service proto",
      "evidence": ["OrderService", "OrderEventService", "Multiple: true"],
      "gate": "make zrpc-proto-compatibility-check",
      "reason": "GenerateRPCFromProto can split multiple services into service-specific output directories when Multiple is enabled.",
      "rollbackOrEscalation": "Rollback by preserving service-specific generated output directories and blocking release if multi-service output disappears."
    },
    {
      "id": "streaming-rpc",
      "status": "supported",
      "surface": "server, client, and bidirectional streaming descriptors",
      "evidence": ["StreamModeServerStream", "StreamModeClientStream", "StreamModeBidiStream"],
      "gate": "make zrpc-proto-compatibility-check",
      "reason": "Generated service descriptors expose stream modes and stream metadata for all three gRPC streaming forms.",
      "rollbackOrEscalation": "Rollback by preserving stream descriptor generation and treating lost stream mode metadata as a breaking candidate."
    },
    {
      "id": "google-well-known-types",
      "status": "supported",
      "surface": "google.protobuf well-known types",
      "evidence": ["google/protobuf/timestamp.proto", "time.Time", "time.Duration", "WKT Go type mapping"],
      "gate": "make zrpc-proto-compatibility-check",
      "reason": "Common google.protobuf well-known types are mapped to stable Go shapes such as time.Time, time.Duration, struct{}, map[string]any, []any, any, and []string.",
      "rollbackOrEscalation": "Rollback by preserving WKT mapping evidence and treating lost WKT type generation as a supported-row regression."
    },
    {
      "id": "client-wrapper",
      "status": "supported",
      "surface": "gofly RPC client wrapper",
      "evidence": ["NewOrderServiceClient", "RuntimeDescriptor", "Stream(ctx context.Context, method string)"],
      "gate": "make zrpc-proto-compatibility-check",
      "reason": "Generated clients include unary wrappers and stream-opening helpers when client generation is enabled.",
      "rollbackOrEscalation": "Rollback by preserving generated client wrappers or documenting NoClient as an explicit compatibility downgrade."
    }
  ],
  "releaseRules": {
    "supportedRegression": "Any supported row that loses generated evidence blocks release.",
    "degradedClaim": "Degraded rows may not be described as full goctl or zRPC parity in docs or release notes.",
    "unsupportedPromotion": "Promotion from degraded to supported requires fixture coverage, generated output evidence, and release gate updates."
  }
}
