global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/rpc-mux-sink-alerts.yaml

scrape_configs:
  - job_name: gofly-observability
    metrics_path: /debug/metrics
    static_configs:
      - targets:
          - host.docker.internal:8081
