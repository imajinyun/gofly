// Package middleware contains production-ready HTTP middleware source files for
// gofly REST services. Each file is intentionally written so it can be copied
// into a gofly-generated project's internal/middleware directory and registered
// with server.Use(...) or a route's Middlewares field.
package middleware
