# Native goctl Oracle Rollback

This fixture is report-only oracle evidence. Roll back by removing the
`nativeorderservice-goctl-native-oracle` row from
`docs/reference/goctl-oracle-replay.json` and keeping the existing gofly replay
matrix unchanged.
