# Task Service Replay Rollback

If this fixture stops compiling or loses generated cache template behavior,
pin the previous `gozero-compatible` generator profile and hold the release.

Rollback requires classifying generated diff output into deterministic repeat,
compatible additions, generated cache template drift, or breaking candidates
before promotion resumes.
